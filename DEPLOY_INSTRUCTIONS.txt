╔══════════════════════════════════════════════════════════════╗
║     DEPLOY VIA RENDER DASHBOARD (CLI not supported)         ║
╚══════════════════════════════════════════════════════════════╝

✅ Code pushed to GitHub: https://github.com/Emperor2004/Smart-Blood-Bank-System

🚀 DEPLOY NOW:

1. Open: https://dashboard.render.com/

2. Create Database:
   New + → PostgreSQL
   - Name: smart-blood-bank-db
   - Database: smart_blood_bank
   - Plan: Free
   - Create Database
   - COPY the Internal Database URL

3. Create Backend:
   New + → Web Service
   - Connect GitHub: Smart-Blood-Bank-System
   - Name: smart-blood-bank-backend
   - Runtime: Docker
   - Dockerfile: Dockerfile.backend.render
   - Plan: Free
   - Add env vars:
     * DATABASE_URL = <paste Internal DB URL>
     * SECRET_KEY = <generate>
     * JWT_SECRET_KEY = <generate>
     * ENVIRONMENT = production
     * DEBUG = False
     * CORS_ORIGINS = *
     * SCHEDULER_ENABLED = False
   - Create Web Service

4. Create Frontend:
   New + → Static Site
   - Connect GitHub: Smart-Blood-Bank-System
   - Name: smart-blood-bank-frontend
   - Build: cd frontend && npm install && npm run build
   - Publish: frontend/dist
   - Add env var:
     * VITE_API_URL = https://smart-blood-bank-backend.onrender.com
   - Create Static Site

5. Setup (in backend Shell):
   alembic upgrade head

6. Create admin (in backend Shell):
   python -c "from app.database import SessionLocal; from app.models import User; from passlib.context import CryptContext; db = SessionLocal(); pwd = CryptContext(schemes=['bcrypt']); admin = User(username='admin', email='admin@hospital.gov.in', hashed_password=pwd.hash('admin123'), role='admin', is_active=True); db.add(admin); db.commit(); print('Admin created')"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💰 COST: Database free 90 days ($7/mo after), Backend & Frontend free forever

📖 Full guide: DEPLOY_DASHBOARD.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
