# Database Configuration
DATABASE_URL=postgresql://bloodbank:bloodbank123@localhost:5432/smart_blood_bank
DB_USER=bloodbank
DB_PASSWORD=bloodbank123
DB_NAME=smart_blood_bank
DB_HOST=localhost
DB_PORT=5432

# Application Configuration
ENVIRONMENT=development
SECRET_KEY=your-secret-key-change-in-production-min-32-chars
DEBUG=True
BACKEND_PORT=8000

# JWT Configuration
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8501

# Forecasting Configuration
FORECAST_HORIZON_DAYS=7
FORECAST_HISTORY_DAYS=180
FORECAST_CONFIDENCE_INTERVAL=0.95

# Transfer Recommendation Configuration
TRANSFER_RADIUS_KM=50
TRANSFER_SURPLUS_THRESHOLD=5
TRANSFER_WEIGHT_EXPIRY=0.6
TRANSFER_WEIGHT_DISTANCE=0.2
TRANSFER_WEIGHT_SURPLUS=0.2
TRANSFER_SPEED_KMH=40

# Expiry Risk Configuration
EXPIRY_RISK_THRESHOLD_DAYS=3

# Donor Eligibility Configuration
DONOR_ELIGIBILITY_DAYS=90

# SMS Gateway Configuration (Twilio)
SMS_GATEWAY_ENABLED=False
SMS_GATEWAY_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Email Configuration (Optional)
EMAIL_ENABLED=False
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@bloodbank.gov.in

# e-RaktKosh Integration (Optional)
ERAKTKOSH_API_ENABLED=False
ERAKTKOSH_API_URL=https://api.eraktkosh.in
ERAKTKOSH_API_KEY=

# Background Jobs Configuration
SCHEDULER_ENABLED=True
FORECAST_JOB_HOUR=2
FORECAST_JOB_MINUTE=0

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Encryption Configuration
ENCRYPTION_KEY=your-encryption-key-for-sensitive-data-32-bytes

# Frontend Configuration (for Streamlit)
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=localhost

# Performance Configuration
MAX_WORKERS=4
CONNECTION_POOL_SIZE=10
CONNECTION_POOL_MAX_OVERFLOW=20

# Feature Flags
ENABLE_MODEL_DRIFT_MONITORING=True
ENABLE_AUDIT_LOGGING=True
ENABLE_GEOSPATIAL_QUERIES=True
